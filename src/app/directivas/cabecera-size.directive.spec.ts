import { CabeceraSizeDirective } from './cabecera-size.directive';

describe('CabeceraSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new CabeceraSizeDirective();
    expect(directive).toBeTruthy();
  });
});
